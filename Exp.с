#include <stdio.h>

double fac(int n)
{
    return (n>0) ? n * fac(n-1) : 1;
}

int main()
{
    double e;
    e = 0;
    for (int n=1; n<6; n++)
    {
        e = e + 1/fac(n);
        if (n>2)
        printf("%.5lf\n", e + 1);
    }    
    return 0;
}
